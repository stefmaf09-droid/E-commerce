# Configuration Exemple
# Copier ce fichier vers .env et remplir avec vos vraies valeurs

# Email Configuration Gmail (pour envoi automatique)
# 💡 Créez un mot de passe d'application sur: https://myaccount.google.com/apppasswords
GMAIL_SENDER=votre-email@gmail.com
GMAIL_APP_PASSWORD=xxxx-xxxx-xxxx-xxxx
SMTP_HOST=smtp.gmail.com
SMTP_PORT=465
FROM_NAME=Agent IA Recouvrement

# Stripe Connect Configuration (pour paiements automatiques)
# 💡 Obtenez vos clés sur: https://dashboard.stripe.com/apikeys
STRIPE_SECRET_KEY=sk_test_xxxxx
STRIPE_PUBLISHABLE_KEY=pk_test_xxxxx
STRIPE_WEBHOOK_SECRET=whsec_xxxxx
STRIPE_MODE=test
PLATFORM_COMMISSION_RATE=0.20

# Database (Production uniquement)
DATABASE_URL=postgresql://user:password@localhost:5432/dbname

# Application
BASE_URL=http://localhost:5000
SECRET_KEY=your-random-secret-key-here

# Shopify OAuth (si vous créez une app)
SHOPIFY_API_KEY=your_shopify_api_key
SHOPIFY_API_SECRET=your_shopify_api_secret

# BigCommerce OAuth (si vous créez une app)
BIGCOMMERCE_CLIENT_ID=your_bigcommerce_client_id
BIGCOMMERCE_CLIENT_SECRET=your_bigcommerce_client_secret

# Rate Limiting (requêtes/seconde)
SCRAPING_RATE_LIMIT=0.5

# Worker Configuration
SYNC_INTERVAL_HOURS=24

# Logging
LOG_LEVEL=INFO
